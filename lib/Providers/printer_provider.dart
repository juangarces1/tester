// lib/providers/printer_provider.dart
import 'package:flutter/cupertino.dart';


class PrinterProvider with ChangeNotifier {
  // BlueDevice? _device;
  // BlueDevice? get device => _device;

  // set device(BlueDevice? d) {
  //   _device = d;
  //   notifyListeners();
  // }
}
